function numberToString(num) {
    //return a string of the number here!
    // const str = String(num);
    // return str;
    if (num > 0) {
        var hello = "hello";
        return hello;
    }
    else {
        var no = "no";
        return no;
    }
}
numberToString(2);
